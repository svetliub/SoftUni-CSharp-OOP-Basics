using System;
using System.Collections.Generic;
using System.Text;

interface IConnectable
{
    void CallNumber(string number);

    void AddNumber(string number);

    void BrowseAddress(string webAddress);

    void AddAddresss(string webAddress);
}